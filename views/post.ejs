<!DOCTYPE html>
<html>

<head>
    <title>Post</title>
</head>

<body>
    <p>
        <a href="/blogs/post">
            <button>Post</button>
        </a>
    </p>
    <% if(files) {%>
        <ol>

            <% files.forEach(file => {%>
                <% file=file.replace(/\.[^/.]+$/, "")%>
                    <li>
                        <a href="/blogs/<%=file%>">
                            <%= file %>
                        </a>
                    </li>

                    <% }); %>
        </ol>
        <% }%>
</body>


</html>